### Learning C threads

A playground repo to store my C threads progress and expermiments


```bash
# Create the binaries directory if it doesn't exist
mkdir -p bin/

# Compile the program
gcc src/main.c -o bin/main

## Running the Program
bin/main 
